package at.j0s.meyercard.app.domain

/**
 * The technique banner on cards 90-109. An enum, not a display string — the
 * technique is data, the wording is Fechtkarte's own and localisable.
 */
enum class Instruction {
    DOUBLE_FEINT,
    MOULINET,
    PROVOKER_TAKER_HITTER,
}
